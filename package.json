{
  "name": "app",
  "version": "1.0.0",
  "main": "backend/app.js",
  "license": "MIT",
  "engines": {
    "node": "16.x",
    "yarn": "1.x"
  },
  "scripts": {
    "install": "(cd frontend && yarn install) & (cd backend && yarn install)",
    "start": "(cd frontend && yarn start) & (cd backend && yarn start)",
    "start:dev": "cd frontend && yarn run start:dev",
    "heroku-postbuild": "(cd frontend && yarn start) & (cd backend && yarn start)"
  }
}

{
  "name": "app",
  "version": "1.0.0",
  "main": "backend/app.js",
  "license": "MIT",
  "engines": {
    "node": "16.x",
    "npm": "8.x",
    "yarn": "1.x"
  },
  "scripts": {
    "install": "cd backend && yarn install",
    "install:dev": "(cd frontend && yarn install) & (cd backend && yarn install)",
    "start": "(cd frontend && yarn start) & (cd backend && yarn start)",
    "dev": "(cd frontend && yarn run dev) & (cd backend && yarn run dev)",
    "production": "cd backend && yarn start",
    "heroku-prebuild": "cd frontend && npm install nuxt",
    "heroku-postbuild": "(cd frontend && yarn start) & (cd backend && yarn start)"
  }
}
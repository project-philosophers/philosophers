{
  "name": "app",
  "version": "1.0.0",
  "main": "backend/app.js",
  "license": "MIT",
  "engines": {
    "node": "16.x",
    "yarn": "1.x"
  },
  "scripts": {
    "build": "cd frontend && yarn build",
    "install": "(cd frontend && yarn install) & (cd backend && yarn install)",
    "start": "(cd frontend && yarn start) & (cd backend && yarn start)",
    "dev": "cd frontend && yarn dev",
    "dev:local": "(cd frontend && yarn dev:local) & (cd backend && yarn dev)",
    "production": "cd backend && yarn run production"
  },
  "dependencies": {}
}
